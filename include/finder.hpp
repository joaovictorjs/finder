#pragma once 

#include <filesystem>
#include <string>
#include <vector>

namespace Finder {
    namespace fs = std::filesystem;
    using str_vec = std::vector<std::string>;

    class Filter {
        private:
        protected:
        public:
    };
}
